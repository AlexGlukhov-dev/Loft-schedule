import {Routes, Route} from 'react-router-dom';

interface NavProps {
    isAuthorized: boolean;
}

const Nav = ({isAuthorized}: NavProps) => {
    return (
        <Routes>

        </Routes>
    )
};

export default Nav;